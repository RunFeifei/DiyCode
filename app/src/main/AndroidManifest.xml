<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.root.okfit"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".OKFitApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".logic.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".logic.BubbleActivity">
        </activity>
        <activity android:name=".logic.SecondActivity">
        </activity>
        <activity android:name=".logic.bus.BusActivity">
        </activity>
    </application>

</manifest>